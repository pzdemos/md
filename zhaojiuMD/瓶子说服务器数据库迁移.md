### 简化缩减表计划
==analytics==
读取点：
  • fc/fc-account/vip-match/index.js (vip-match 会去 analytics 里查 eventName: 'vipMatchTime' 的历史记录)
  • fc/fc-admin/pages/match-time-review/index.js
  • fc/fc-admin/pages/green-rate-history/index.js
  
==pairHistory==
fc/fc-admin/pages/pair-chat-analysis/index.js（约 21 处引用，路由前缀
    /admin/pair-chat-analysis）；主要接口有 POST /api/analyze、POST
    /api/get-messages、POST /api/export、POST
    /api/analyze-trend；这是目前最重度的 pairHistory 分析页之一，会按
    createAt、chatCount、注册时间等条件筛选，再按 chatId
    分组做配对聊天质量分析。
  • fc/fc-admin/pages/female-new-user-pair-quality/index.js（约 13
    处引用，路由前缀 /admin/female-new-user-pair-quality）；主要接口有 POST
    /data、POST /pair-chat-details、POST /recent-pairs；重度读取 pairHistory
     做女性新用户配对质量、有效聊天、对方画像等分析。
  • fc/fc-admin/pages/model-test-users/index.js（约 9 处引用，路由前缀
    /admin/model-test-users）；主要接口有 POST /api/get-female-users、GET
    /api/test-users-chat-success-rate、GET
    /api/get-user-pair-history；会直接从 pairHistory
    拉测试用户最近配对记录、成功率和个人配对历史。
  • fc/fc-admin/pages/all-user-pair-time/index.js（约 7 处引用，路由前缀
    /admin/all-user-pair-time）；主要接口有 POST /get-pair-time-stats、POST
    /record-pair-analytics、POST /get-analytics-data；这里用 pairHistory
    做真实配对耗时统计，区分普通用户、新女用户、VIP、SVIP。
  • fc/fc-admin/pages/female-new-user-pair-quality/female-old-user-pair-anal
    ysis/index.js（约 6 处引用，路由前缀 /admin/female-new-user-pair-quality
    /female-old-user-pair-analysis）；主要接口有 GET
    /online-old-female-users、POST /user-pair-history、POST
    /pair-chat-messages；重度读取 pairHistory
    做一年以上老女用户的配对历史和长对话分析。
  • fc/fc-admin/pages/pair-time-analytics/index.js（约 5 处引用，路由前缀
    /admin/pair-time-analytics）；主要接口有 GET /api/pair-data、GET
    /api/pair-data-single-day；按天统计新用户配对数，主数据源就是
    pairHistory。
  • fc/fc-admin/pages/pair-time-analytics/v2/index.js（约 5 处引用，路由前缀
     /admin/pair-time-analytics/v2）；主要接口有 GET
    /api/comparison-data、GET /api/speed-data；专门从 pairHistory
    做女性新用户配对速度分析和分钟级采样统计。
  • fc/fc-admin/pages/new-user-usage-record/v4/index.js（约 4
    处引用，路由前缀
    /admin/new-user-usage-record-v4）；这是综合分析页，文件里有多处
    pairHistory 查询，用于首日匹配次数、首配分析、聊天阶段分布、24
    小时流失分布等多个接口。
  • fc/fc-admin/pages/new-user-usage-record/v3/index.js（约 4
    处引用，路由前缀 /admin/new-user-usage-record-v3）；和 v4
    类似，是上一版综合分析页，也大量依赖 pairHistory。
  • fc/fc-admin/pages/new-user-usage-record/v2/index.js（约 4
    处引用，路由前缀
    /admin/new-user-usage-record-v2）；也是同类旧版分析页，仍然大量读
    pairHistory。
  • fc/fc-admin/pages/female-new-user-pair-speed/index.js（约 3
    处引用，路由前缀 /admin/female-new-user-pair-speed）；主要接口有 GET
    /api/pair-data/:hour、GET /api/pair-data、POST
    /api/auto-adjust-filter；用 pairHistory 分析新女用户配对速度。

  • fc/fc-admin/pages/new-user-usage-record/index.js（约 2 处引用，路由前缀
    /admin/new-user-usage-record）；用 pairHistory 算用户 24
    小时内匹配次数和次日留存。
  • fc/fc-admin/pages/new-user-usage-report/index.js（约 1 处引用，路由前缀
    /admin/new-user-usage-report）；接口 GET /api/data；批量拉 pairHistory
    做新用户使用报表。
  • fc/fc-admin/pages/new-girl-valid-chat-analytics/index.js（约 1
    处引用，路由前缀 /admin/new-girl-valid-chat-analytics）；主要接口有 GET
    /female-users、GET /female-stats、GET /ad-sources；有一部分统计会结合
    pairHistory。

### 数据盘结构
   
```shell
# 当前挂载状态
df -h
==================================================
Filesystem      Size  Used Avail Use% Mounted on
devtmpfs        7.8G     0  7.8G   0% /dev
tmpfs           7.8G  4.0K  7.8G   1% /dev/shm
tmpfs           7.8G  233M  7.6G   3% /run
tmpfs           7.8G     0  7.8G   0% /sys/fs/cgroup
/dev/vda1        40G  6.5G   31G  18% /

# 需迁移节点
===========================================
/dev/vdb1       3.5T  3.2T   77G  98% /mnt
===========================================
```

### 测试迁移服务

- ecs.n1.xlarge实例 青岛区 按量付费实例 建议 8 核  16G （CentOS 7.0 64位）
- 安装 mongodb 3.6.3
- 创建迁移快照创建云盘 绑定在新实例 开启主节点
- 创建备份节点服务开启隐节点同步主节点
- 挂载 **≥4 TB ESSD PL1** 数据盘
-  确保与旧 ECS 在同一 VPC（或可内网互访）
### 挂载磁盘流程 

```shell
#查看磁盘情况
lsblk
# 创建挂载目录
mkdir /mnt/data
# 挂载
mount /dev/nvme1n1 /mnt/data
```

### 单节点转副本集

==注意== ：组成副本集（尤其是多节点）时，**节点之间必须认证**。  MongoDB 使用一个 **keyFile** 来在成员之间共享密钥，本次是在 一个实例上运行两个节点，所以可只创建一个 ，创建如下：
```shell
openssl rand -base64 756 > /mongodb/keyfile 
chmod 400 /mongodb/keyfile
```
创建后需要通过内网传输 ==keyfile== 到隐节点

#### 多节点运行 
----mongodb 3.6 
``` shell
# 主节点
mongod --dbpath /mnt/db2/mongodb \
       --logpath /mnt/db2/mongodb/mongod.log \
       --port 27017 \
       --bind_ip 0.0.0.0 \
       --replSet rs0 \
       --keyFile /mongodb/keyfile \
       --auth \
       --fork
       
       
       
# 从节点
#==注意==： 从节点需要保持目录干净，否则影响同步
# 确保执行 下列命令 /mnt/data/mongodb 是空的
rm rf /mnt/data2/mongodb/*

mongod --dbpath /mnt/data/mongodb \
       --logpath /mnt/data/mongodb/mongod.log \
       --port 27017 \
       --bind_ip 0.0.0.0 \
       --replSet rs0 \
       --keyFile /mongodb/keyfile \
       --auth \
       --fork

```

==注意== 如果下列运行出现错误(一般新机不会，指从未运行过mongodb) 需要检查 *mongodb-xxxx.sock* 文件，一般在 ==/tmp== 目录下,删除了锁文件后重新启动
```shell
cd /tmp
ls
#删除了对应锁文件
rm -rf mongo-xxx.sock
```

#### 查看运行状态并连接初始化

```shell
ps aux｜grep mongod
#出现刚刚运行进程即为成功
root     25181  8.2 11.6 3505700 1855856 ?     SLl  14:37   5:07 mongod ...
root     26263  106 17.8 4281060 2855484 ?     SLl  14:48  55:08 mongod ...
root     29096  0.0  0.0 112812   980 pts/0    S+   15:39   0:00 

# 连接测试，原有数据库内容未改动，以原有方式连接
mongo --port 40000 -u <用户名> -p <密码> --authenticationDatabase admin

# 提示符是 `rs0:SECONDARY>`，这意味着当前节点处于 **Secondary** 状态，还不是 Primary
>>rs0:SECONDARY>

# 节点配置未生效 执行:
rs.conf() 

# 初始化主节点
# 注意 此处初始化host 可以使用 hostname 直接控制台执行 hostname 获取替换 127.0.0.1
# 但是 双节点必须保持一致，即 主节点使用 127.0.0.1 ，从、隐节点 需要保持一致
rs.initiate({
  _id: "rs0",
  members: [
    { _id: 0, host: "127.0.0.1:27017" }
  ]
})
# 初始化成功
>>rs0:PRIMARY>
# 添加隐节点 此时在不同主机上 但处于同一内网环境下
rs.add({
    host: "内网地址:40001",
    priority: 0,
    hidden: true,
    votes: 0
})

#退出主节点 shell 连接
#连接隐节点
mongo --port 40001 -u <用户名> -p <密码> --authenticationDatabase admin

#出现 下列情况表示 正在同步 
>> rs0:STARTUP2> 
# 可查看进程情况 
top
#出现 mongod 高占用 情况 是正常 等待一段时间 连接出现 
#提示符是 `rs0:SECONDARY>` 表明同步完成

```

